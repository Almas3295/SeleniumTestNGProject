package Demo;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

public class SeleniumLocatorsHM {

	public static void main(String[] args) throws InterruptedException {
  
  
  WebDriver driver=new ChromeDriver();
  
  driver.get("https://login.salesforce.com/");
  
  
 // driver.findElement(By.id("user name")).sendKeys("test1");

//  driver.findElement(By.name("pw")).sendKeys("123");
  
  //driver.findElement(By.id("Login")).click();
  
 // Thread.sleep(2000);
  
  //driver.findElement(By.linkText("Forgot Your Password?")).click();
   
 // driver.findElement(By.name("cancel")).click();
   
 // driver.findElement(By.linkText("Use Custom Domain")).click();
  
 // driver.findElement(By.linkText("Use Custom Domain")).click();
 
 // driver.findElement(By.linkText("small")).click();
   
   
  
  
 }

}
